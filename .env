# Public Expo client config (bundled into the app — NO secrets here).
# Points the client at the deployed Supabase Edge Functions.
# See README.md → "Backend proxy".
EXPO_PUBLIC_API_URL=https://mrhqsskgnkicutvozzju.supabase.co/functions/v1

# Sign-in (Supabase Auth). Both are PUBLIC values safe to ship in the client.
# The anon key is NOT a secret — copy it from:
#   Supabase dashboard → Project Settings → API → "anon public" key.
EXPO_PUBLIC_SUPABASE_URL=https://mrhqsskgnkicutvozzju.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=PASTE_YOUR_ANON_PUBLIC_KEY_HERE
